import getMuiTheme from 'material-ui/styles/getMuiTheme';
import { teal500, teal400 } from 'material-ui/styles/colors';

export default getMuiTheme({
  palette: {
    primary1Color: teal400,
    primary2Color: teal500,
  },
});
